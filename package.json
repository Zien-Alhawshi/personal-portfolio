{
  "name": "project",
  "dependencies": {
    "vite": "^5.4.21"
  },
  "scripts": {
    "start": "npx vite",
    "dev": "npx vite",
    "build": "npm install -g vite && vite build",
    "preview": "npx vite preview"
  }
}